# Optional — enables AI phrasing in the Assistant (server-side only, never exposed to the browser)
GEMINI_API_KEY=

# Optional — Mappls (MapmyIndia) sponsor basemap key (client-side)
NEXT_PUBLIC_MAPPLS_KEY=
